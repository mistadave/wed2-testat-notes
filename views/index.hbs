<div class="navigation">
<h3>Willkommen! Zu der besten Note App auf der Welt! Juhuu</h3>
<div class="images">
    <img src='/images/note.jpg'>
</div>
    <form>
        <div class="">
            <button class="button" type='submit' formaction="/notes/create" formmethod="get">Create new Note</button>
            <button class="button right" type="submit" formaction="/changeStyle" formmethod="get">Style-Switcher
            </button>
        </div>
        <div class="">
            <button class="button" type="submit" formaction="/showByFinishDate" formmethod="get">By finish Date</button>
            <button class="button" type="submit" formaction="/showByCreatedDate" formmethod="get">By created Date
            </button>
            <button class="button" type="submit" formaction="/showByImportance" formmethod="get">By Importance</button>
            <button class="button" type="submit" fomaction="/showFinished" formmethod="get">Show finished</button>
        </div>
    </form>
</div>

<div class="notes">
<h3>Notes</h3>
    <form>
        <!-- TODO: if not working and why empty already 4 iterations? -->
        {{#if notes}}
            {{#each notes}}
                <div class="inline-box">
                    <div class="col-md-2">
                        <input type="checkbox"><label>Finished</label>
                    </div>
                    <div class="col-10">
                        {{title}}
                        {{importance}}
                        <input type="text" value="{{description}}" readonly>
                    </div>
                    <div class="">
                        <button class="button" type="submit" formaction="/editNote" formmethod="post">Edit</button>
                    </div>
                </div>
            {{/each}}
        {{else}}
            <div class="">No data yet</div>
        {{/if}}
    </form>
</div>